1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。


1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。
1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。
1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。
1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。
1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。





1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。
1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(<color=red>特殊父节点除外</color>)。



1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。

1.在每个节点中，通过单机节点右方接口可拖曳出一条线，连接该线的节点将会作为该节点的子节点。
2.单击节点左方接口可删除连接至该节点的父节点(特殊父节点除外)。